Categories <%= link_to "New", new_category_path %>

<% @categories.each do |category| %>
  <div><%= category.name %> <%= link_to "Edit", edit_category_path(category) %> <%= link_to "Delete", category_path(category), method: :delete %></div>

<% end %>
