<%= form_for @post do |f|%>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :content %>
  <%= f.text_area(:content, size: "50x6") %><br>
  <%= f.label :category_id %>
  <%= collection_select :post, :category_id, Category.all, :id, :name %>
  <%= f.submit %>
<% end %>
